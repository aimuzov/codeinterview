module.exports.TREE = {
	a: {
		b: 1,
		c: { d: 0, e: 1 },
		f: 0,
	},
	g: 0,
	h: 1,
}

/**
 * Необходимо написать функцию, которая инвертирует все значения в дереве (1 меняет на 0 и наоборот)
 * Результат работы функции - новый экземпляр дерева с инвертированными
 * значениями (дерево на входе остается неизменным).
 *
 * @param {Object} tree
 * @returns {Object}
 */
module.exports.skillcheckTreeInversion = function (tree = TREE) {
	// TODO: Необходимо реализовать
}
